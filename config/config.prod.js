'use strict';

module.exports = appInfo => {
  const config = {};

  /**
   * @member Config#
   * @property {String} KEY - description
   */
  config.imagePath = "/usr/local/pic/"

  return config;
};
